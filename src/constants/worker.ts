export const WORKER_IDLE_TIMEOUT_MS = 300_000
export const MIN_WORKER_STAGGER_MS = 1000 // Delay between spawning the first MIN workers
export const DYNAMIC_SCALE_DELAY_MS = 500 // Delay before opening a dynamic worker above MIN

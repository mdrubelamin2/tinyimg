/** Warn when session storage estimate exceeds this fraction of quota. */
export const QUOTA_WARN_RATIO = 0.8

/** First N bytes used for duplicate detection when adding files. */
export const DEDUP_HASH_SLICE_BYTES = 65_536
